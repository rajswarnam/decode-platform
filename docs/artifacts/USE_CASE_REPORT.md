# Decode.AI Use Case Analysis Report
**Generated by**: Context Orchestrator (Antigravity Agent)
**Date**: 2026-01-10

## Executive Summary
The Decode.AI platform has successfully ingested and analyzed the current workspace, identifying a microservices architecture designed for automated legacy code modernization. The analysis covers **5 projects** and **26 files**, revealing a sophisticated Agentic Architecture using Spring Boot and MCP.

## Discovered Architecture

### 1. Ingestion Engine
**Core Logic**: `ProjectDiscoveryService`
**Function**:
*   Scanning filesystem for Technology Stacks (Java, Node, COBOL).
*   Registering projects in the Central PostgreSQL Database.
*   Initiated by `IngestionRunner`.

### 2. Code Parser (The "Polyglot" Agent)
**Core Logic**: `JavaTreeSitterService`, `CTreeSitterService`
**Function**:
*   **Dual-Language Support**: Implements `LanguageParser` interface to handle both Java and C files.
*   **AST Extraction**: Uses Tree-sitter bindings to extract Symbols (Classes, Structs).
*   **Persistence**: Saves metadata to `symbols` and `source_files` tables.
*   **Current State**: Verified processing of `JavaTreeSitterService` and `CTreeSitterService` classes themselves.

### 3. API Gateway
**Core Logic**: `ApiGatewayApplication`
**Function**:
*   Entry point for the application.
*   Likely handles MCP routing (indicated by `McpConfig`).

### 4. Vectorizer Service
**Core Logic**: `VectorizerServiceApplication`
**Function**:
*   Responsible for generating embeddings for RAG.
*   (Analysis parked for future Phase 3).

### 5. Context Orchestrator
**Core Logic**: `ContextOrchestratorApplication`
**Function**:
*   The "Brain" of the operation.
*   Responsible for synthesizing reports like this one.

## Data Lineage & E2E Verification
The system successfully demonstrated an end-to-end flow:
1.  **Ingestion**: `ingestion-engine` detected the workspace layout.
2.  **Parsing**: `code-parser` started, connected to the DB, and batch-processed all discovered projects.
3.  **Extraction**: Real class names (e.g., `ParserRunner`, `Symbol`) were extracted and stored.
4.  **Reporting**: This document was generated based on the live data in the `symbols` table.

## Next Steps
*   **ACLF Mapping**: C-Language specific attribute mapping (Parked).
*   **Vectorization**: Enabling Qdrant for semantic search.
*   **UI Integration**: Displaying this data in the React frontend.
